{
  "name": "student-assist-storage-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-student-assist-storage --config ../firebase.rules-test.json --only firestore,storage \"node --test storage.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.3.0"
  }
}
